server {
    listen 8080;
    server_name _;

    root /usr/share/nginx/html;
    index booking.html;

    # 單頁應用或直接訪問子路徑時回退到 booking.html
    location / {
        try_files $uri $uri/ /booking.html;
    }
}
